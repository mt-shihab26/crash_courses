const Heading2 = ({ children }) => {
    return (
        <div>
            <h2 className="border-b border-[#BBBBBB] pb-[5px] font-bold">
                {children}
            </h2>
        </div>
    );
};

export default Heading2;
